[tool.pyright]
include = [".", "app"]
exclude = ["**/node_modules", "**/__pycache__", ".venv", "venv"]
extraPaths = [".", "app"]
venvPath = "."
venv = ".venv"
pythonVersion = "3.12"
typeCheckingMode = "basic"

[tool.pytest.ini_options]
pythonpath = [".", "app"]
testpaths = ["tests"]
