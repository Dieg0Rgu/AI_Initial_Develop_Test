{
  "name": "gastroteacher-fullstack",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "cd frontend/Rage_frontend && npm install && npm run build",
    "dev:frontend": "cd frontend/Rage_frontend && npm run dev",
    "dev:backend": "cd backend && uvicorn app.main:app --reload"
  }
}
